# Task: sum the numbers in a string

You are given the text `10 20 30 40` — whitespace-separated integers in a single
string. Convert each piece to an integer and print their sum.

(Converting text to an integer is a standard-library operation — find the right
module and function rather than writing your own parser.)

Print nothing else.

## Exact expected output

```
100
```
