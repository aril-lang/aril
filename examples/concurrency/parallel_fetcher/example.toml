description = "Structured-concurrent fetch with per-request deadline + scope cancellation"
source   = "services parallel_fetcher"
category = "concurrency"
showcase = true
forces   = []   # TODO backfill against lang-spec artefact IDs
since    = "0.1"
status   = "running"
expects  = "run-pass"
entry    = "parallel_fetcher.aril"
expected_output = "expected-output.txt"
