--- rate_limited.aril
+++ rate_limited.aril
@@ -25,6 +25,7 @@
 }
 
 func main() {
+  break
   let deadline = makeChannel<unit>(0)
   let ticks    = time.tick(time.milliseconds(50))
   let buffer   = makeChannel<int>(4)
