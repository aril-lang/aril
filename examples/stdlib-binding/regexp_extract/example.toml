description = "regexp match/extract via the regexp.Regexp value handle"
source   = "stdlib-coverage demand: regexp"
category = "stdlib-binding"
showcase = false
forces   = []
since    = "0.1"
status   = "running"
expects  = "run-pass"
entry    = "regexp_extract.aril"
expected_output = "expected-output.txt"

[[error]]
patch   = "errors/unknown-handle-member.patch"   # re.noSuchMethod — absent from the regexp.Regexp handle method set
expect  = ["E0214"]
stage   = "sema"
matches = "errors/unknown-handle-member.expected"
origin  = "synthetic"

# E0218 — an obtain-only stdlib handle is never a `{}` literal; get it from the
# constructor (regexp.mustCompile). Diagnosed in Aril coordinates (D10).
[[error]]
patch   = "errors/obtain-only-handle.patch"
expect  = ["E0218"]
stage   = "sema"
matches = "errors/obtain-only-handle.expected"
origin  = "synthetic"
