description = "strings.trimLeft cutset-trim — strip insignificant leading zeros (set-trim, vs fixed-substring trimPrefix)"
source   = "SURFACE-ERGONOMICS PR5"
category = "stdlib-binding"
showcase = false
forces   = []
since    = "0.4"
status   = "running"
expects  = "run-pass"
entry    = "normalize_digits.aril"
expected_output = "expected-output.txt"

# E0217 — an unbound member on a bound stdlib namespace (`strings.stripZeros`)
# is diagnosed in Aril coordinates, not leaked as a raw `go build` "undefined:
# strings.stripZeros" (D10).
[[error]]
patch   = "errors/unbound-strings-member.patch"
expect  = ["E0217"]
stage   = "sema"
matches = "errors/unbound-strings-member.expected"
origin  = "synthetic"
