description = "time.Duration arithmetic + rendering (d.add / d.mul / d.string)"
source   = "stdlib-coverage demand: time.Duration"
category = "stdlib-binding"
showcase = false
forces   = []
since    = "0.1"
status   = "running"
expects  = "run-pass"
entry    = "duration_budget.aril"
expected_output = "expected-output.txt"
