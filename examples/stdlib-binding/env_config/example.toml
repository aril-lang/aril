description = "os.lookupEnv → Option<string> (unset vs empty) with a default fallback"
source   = "stdlib-coverage env_config"
category = "stdlib-binding"
showcase = false
forces   = []   # TODO backfill against lang-spec artefact IDs
since    = "0.1"
status   = "running"
expects  = "run-pass"
entry    = "env_config.aril"
expected_output = "expected-output.txt"

[[error]]
patch   = "errors/compare-option-to-none.patch"   # `v == None` — an Option is inspected with match, not compared
expect  = ["E0401"]
stage   = "sema"
matches = "errors/compare-option-to-none.expected"
origin  = "synthetic"
