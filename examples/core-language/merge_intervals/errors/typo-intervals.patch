--- merge_intervals.aril
+++ merge_intervals.aril
@@ -14,7 +14,7 @@
 }
 
 func merge(intervals: []Interval): []Interval {
-  if intervals.len() == 0 {
+  if interval.len() == 0 {
     return []Interval{}
   }
 
