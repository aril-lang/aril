--- valid_parentheses.aril
+++ valid_parentheses.aril
@@ -42,6 +42,7 @@
 }
 
 func main() {
+  break
   let cases = ["()", "()[]{}", "(]", "([)]", "{[]}", ""]
   for s in cases {
     fmt.println(s, "->", isValid(s))
