--- valid_parentheses.aril
+++ valid_parentheses.aril
@@ -25,6 +25,7 @@
 
 func isValid(s: string): bool {
   var stack = Stack<rune>{}
+  let _ = try opener(s)
   for c in s {
     if isOpener(c) {
       stack.push(c)
