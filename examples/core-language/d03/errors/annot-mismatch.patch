--- d03.aril
+++ d03.aril
@@ -28,7 +28,7 @@
     let s = strings.trimSpace(ln)
     if s.len() == 0 { continue }
     let line = List<int>{}
-    var i = 0
+    var i: string = 0
     while i < s.len() {
       let n = try strconv.atoi(s[i:i+1])
       line.push(n)
