--- d04.aril
+++ d04.aril
@@ -19,7 +19,7 @@
   for ln in strings.split(text, "\n") {
     let s = strings.trimSpace(ln)
     if s.len() == 0 { continue }
-    rows = rows.push(s.bytes())
+    rows.push(s.bytes())
   }
   return Ok(rows)
 }
