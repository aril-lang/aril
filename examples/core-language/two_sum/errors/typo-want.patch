--- two_sum.aril
+++ two_sum.aril
@@ -10,7 +10,7 @@
   var seen = Map<int, int>{}      // value -> its index in `nums`
   for (i, n) in nums {
     let want = target - n
-    match seen.get(want) {
+    match seen.get(wnat) {
       Some(j) => return Some((j, i)),
       None    => seen.set(n, i),
     }
