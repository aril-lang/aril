--- two_sum.aril
+++ two_sum.aril
@@ -23,7 +23,7 @@
   let target = 9
 
   match twoSum(nums, target) {
-    Some((i, j)) => fmt.println("indices:", i, j),
+    Some((i, j)) => fmt.println("indices:" i, j),
     None         => fmt.println("no pair"),
   }
 }
